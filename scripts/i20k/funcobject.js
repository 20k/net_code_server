function(context, args)
{
	function debug()
	{
		print("debugging");
	}
	
	print("function object");
	
	//return #ms.accts.xfer_gc_to({to:"test_user2", amount:1});
	
	
	
	//return JSON.stringify(args.f.call());
	
	//return args.f.call({to:"test_user2", amount:1});
	
	//return args.f.call({to:"test_user2", amount:1});
}